enum ErrorType { auth, network, cache, location, normal }

enum SuccessType { auth, network, normal }

enum SocialAuthType { google, facebook }

enum VerificationMethod { mail, sms }

enum GenderType { male, female }
